[build]
  command = """
  git clone https://github.com/flutter/flutter.git --depth 1 --branch 3.47.1
  export PATH="$PWD/flutter/bin:$PATH"
  flutter config --enable-web
  flutter pub get
  flutter build web --release
  """
  publish = "build/web"